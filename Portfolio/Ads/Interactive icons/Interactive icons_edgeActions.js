/***********************
* Adobe Edge Animate Composition Actions
*
* Edit this file with caution, being careful to preserve 
* function signatures and comments starting with 'Edge' to maintain the 
* ability to interact with these actions from within Adobe Edge Animate
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // aliases for commonly used Edge classes

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      

      

      Symbol.bindElementAction(compId, symbolName, "${cent2_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         
         sym.getSymbol("cent2_symbol_1").play();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${cent2_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         sym.getSymbol("cent2_symbol_1").stop();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${shesterni_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("shesterni_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${shesterni_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         sym.getSymbol("shesterni_symbol_1").stop();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Dollars_anim}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         
         sym.getSymbol("Dollars_anim").play();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Dollars_anim}", "mouseout", function(sym, e) {
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("Dollars_anim").stop('start');
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Location_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Location_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Location_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         sym.getSymbol("Location_symbol_1").stop();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${smoke2_symbol_1}", "mouseover", function(sym, e) {
         
         sym.getSymbol("smoke2_symbol_1").play();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${smoke2_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("smoke2_symbol_1").stop('first_start');
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${all_server_23}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("smoke2_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${all_server_23}", "mouseout", function(sym, e) {
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("smoke2_symbol_1").stop('first_start');

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${yellow_arrow-01_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("yellow_arrow-01_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${yellow_arrow-01_symbol_1}", "mouseout", function(sym, e) {
         
         sym.getSymbol("yellow_arrow-01_symbol_1").stop();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Rocket2_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("Rocket2_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Rocket2_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         sym.getSymbol("Rocket2_symbol_1").stop();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${cashe-01_symbol_1}", "mouseover", function(sym, e) {
         
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("cashe-01_symbol_1").play('start');

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${cashe-01_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("cashe-01_symbol_1").stop('stop');
         

      });
      //Edge binding end

      

      

      Symbol.bindElementAction(compId, symbolName, "${lamp_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("lamp_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${lamp_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("lamp_symbol_1").stop('start');
         

      });
      //Edge binding end

      

      

      

      

      

      

      Symbol.bindElementAction(compId, symbolName, "${upgrade_fps4_symbol_1}", "mouseover", function(sym, e) {
         
         sym.getSymbol("upgrade_fps4_symbol_1").play();

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${upgrade_fps4_symbol_1}", "mouseout", function(sym, e) {
         
         sym.getSymbol("upgrade_fps4_symbol_1").play('rev');
         
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${office365-013_symbol_1}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         sym.getSymbol("office365-013_symbol_1").play();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${office365-013_symbol_1}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("office365-013_symbol_1").stop('start');
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${lock}", "mouseover", function(sym, e) {
         // insert code to be run when the mouse hovers over the object
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.getSymbol("lock").getSymbol("ru4k-01_symbol_1").play('start');
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${lock}", "mouseout", function(sym, e) {
         // insert code to be run when the mouse is moved off the object
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.getSymbol("lock").getSymbol("ru4k-01_symbol_1").stop('start');
         

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         // insert code here
      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'cent2_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 750, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("cent2_symbol_1");
   //Edge symbol end:'cent2_symbol_1'

   //=========================================================
   
   //Edge symbol: 'Rocket2_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 791, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("Rocket2_symbol_1");
   //Edge symbol end:'Rocket2_symbol_1'

   //=========================================================
   
   //Edge symbol: 'Location_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 791, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("Location_symbol_1");
   //Edge symbol end:'Location_symbol_1'

   //=========================================================
   
   //Edge symbol: 'yellow_arrow-01_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 791, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("yellow_arrow-01_symbol_1");
   //Edge symbol end:'yellow_arrow-01_symbol_1'

   //=========================================================
   
   //Edge symbol: 'shesterni_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 458, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("shesterni_symbol_1");
   //Edge symbol end:'shesterni_symbol_1'

   //=========================================================
   
   //Edge symbol: 'smoke2_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 625, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("smoke2_symbol_1");
   //Edge symbol end:'smoke2_symbol_1'

   //=========================================================
   
   //Edge symbol: 'S-012_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 708, function(sym, e) {
         // insert code here
         sym.play('start');

      });
      //Edge binding end

   })("S-012_symbol_1");
   //Edge symbol end:'S-012_symbol_1'

   //=========================================================
   
   //Edge symbol: 'Dollars_anim'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1500, function(sym, e) {
         
         sym.play();
         

      });
      //Edge binding end

   })("Dollars_anim");
   //Edge symbol end:'Dollars_anim'

   //=========================================================
   
   //Edge symbol: 'cashe-01_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 791, function(sym, e) {
         // insert code here
         sym.stop();

      });
      //Edge binding end

   })("cashe-01_symbol_1");
   //Edge symbol end:'cashe-01_symbol_1'

   //=========================================================
   
   //Edge symbol: 'lamp_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 292, function(sym, e) {
         // insert code here
         // Play the timeline at a label or specific time. For example:
         // sym.play(500); or sym.play("myLabel");
         sym.play('start');

      });
      //Edge binding end

   })("lamp_symbol_1");
   //Edge symbol end:'lamp_symbol_1'

   //=========================================================
   
   //Edge symbol: 'office365-012_symbol_1'
   (function(symbolName) {   
   
   })("office365-012_symbol_1");
   //Edge symbol end:'office365-012_symbol_1'

   //=========================================================
   
   //Edge symbol: 'upgrade_fps4_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 625, function(sym, e) {
         // insert code here
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop();

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 1185, function(sym, e) {
         
         // Go to a label or specific time and stop. For example:
         // sym.stop(500); or sym.stop("myLabel");
         sym.stop('stop');

      });
      //Edge binding end

      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 0, function(sym, e) {
         // insert code here
         sym.play();

      });
      //Edge binding end

   })("upgrade_fps4_symbol_1");
   //Edge symbol end:'upgrade_fps4_symbol_1'

   //=========================================================
   
   //Edge symbol: 'office365-013_symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 708, function(sym, e) {
         // insert code here
         sym.stop();

      });
      //Edge binding end

   })("office365-013_symbol_1");
   //Edge symbol end:'office365-013_symbol_1'

   //=========================================================
   
   //Edge symbol: 'ru4k-01_symbol_1'
   (function(symbolName) {   
   
   })("ru4k-01_symbol_1");
   //Edge symbol end:'ru4k-01_symbol_1'

   //=========================================================
   
   //Edge symbol: 'lock'
   (function(symbolName) {   
   
   })("lock");
   //Edge symbol end:'lock'

})(window.jQuery || AdobeEdge.$, AdobeEdge, "EDGE-920607168");